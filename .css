/* Profile Grid: Bilkul Square Photos */
#userPostGrid {
    display: grid; 
    grid-template-columns: repeat(3, 1fr); 
    gap: 2px; 
    margin-top: 20px;
}

#userPostGrid img {
    width: 100%;
    aspect-ratio: 1 / 1; /* Photo ko hamesha square rakhega */
    object-fit: cover;
}

/* Reels View: Smooth Snap Effect */
#reelsView {
    display: none; /* JS isse show karega */
    scroll-snap-type: y mandatory;
    overflow-y: scroll;
    height: 100vh;
    width: 100%;
    background: #000;
    scrollbar-width: none; /* Firefox ke liye scrollbar hide */
    -ms-overflow-style: none; /* IE ke liye */
    -webkit-overflow-scrolling: touch; /* iOS par smooth scrolling */
}

/* Chrome aur Safari se scrollbar hatane ke liye */
#reelsView::-webkit-scrollbar {
    display: none;
}

.reel-video-container {
    width: 100%;
    height: 100vh; 
    scroll-snap-align: start;
    scroll-snap-stop: always;
    position: relative;
    background: #000;
}

.reel-video-container video {
    width: 100%;
    height: 100%;
    object-fit: cover; /* Video poori screen cover karegi */
}
