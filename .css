body {
    margin: 0;
    height: 100vh;
    display: flex;
    justify-content: center;
    align-items: center;
    background: linear-gradient(45deg, #3498db, #8e44ad);
    font-family: 'Segoe UI', sans-serif;
    overflow: hidden;
}

#animation-container {
    position: absolute;
    font-size: 80px;
}

/* Glassmorphism Effect */
.glass-card {
    background: rgba(255, 255, 255, 0.15);
    backdrop-filter: blur(15px);
    -webkit-backdrop-filter: blur(15px);
    border: 1px solid rgba(255, 255, 255, 0.2);
    padding: 2.5rem;
    border-radius: 25px;
    box-shadow: 0 15px 35px rgba(0,0,0,0.2);
    width: 320px;
    text-align: center;
    color: white;
    visibility: hidden; /* GSAP will show it */
}

.login-logo { font-size: 50px; margin-bottom: 10px; }
.login-title { margin: 0; font-size: 28px; letter-spacing: 2px; }

.login-btn {
    width: 100%;
    padding: 12px;
    margin: 10px 0;
    border: none;
    border-radius: 10px;
    background: white;
    color: #333;
    font-weight: bold;
    cursor: pointer;
    transition: transform 0.2s;
}

.login-btn:hover { transform: scale(1.03); }

.login-divider { margin: 15px 0; font-size: 12px; opacity: 0.7; }

.social-btns { display: flex; justify-content: center; gap: 20px; }
.icon-btn { 
    background: rgba(255,255,255,0.2); 
    border: none; 
    font-size: 24px; 
    padding: 10px; 
    border-radius: 50%; 
    cursor: pointer; 
}
