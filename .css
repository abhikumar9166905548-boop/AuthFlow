/* --- 1. COMMENT SHEET (Instagram Style Bottom Sheet) --- */
.comment-sheet {
    position: fixed;
    bottom: -100%; /* Shuru mein niche chupaa rahega */
    left: 0;
    width: 100%;
    height: 70vh;
    background: #262626;
    border-radius: 15px 15px 0 0;
    z-index: 2005;
    transition: bottom 0.3s ease-out;
    display: flex;
    flex-direction: column;
}

.comment-sheet.active {
    bottom: 0; /* JS se 'active' class aane par slide up hoga */
}

.sheet-header {
    padding: 15px;
    text-align: center;
    border-bottom: 0.5px solid #3d3d3d;
    position: relative;
}

.drag-handle {
    width: 40px;
    height: 4px;
    background: #555;
    border-radius: 2px;
    margin: 0 auto 10px;
}

.sheet-header i {
    position: absolute;
    right: 15px;
    top: 20px;
    cursor: pointer;
}

.comment-list {
    flex: 1;
    overflow-y: auto;
    padding: 15px;
}

.comment-item {
    display: flex;
    gap: 12px;
    margin-bottom: 15px;
    font-size: 14px;
}

.comment-item img {
    width: 32px;
    height: 32px;
    border-radius: 50%;
}

.comment-input-area {
    padding: 15px;
    border-top: 0.5px solid #3d3d3d;
    display: flex;
    align-items: center;
    gap: 10px;
    background: #262626;
}

.user-mini-pfp {
    width: 35px;
    height: 35px;
    border-radius: 50%;
}

#newCommentInput {
    flex: 1;
    background: transparent;
    border: none;
    color: white;
    outline: none;
    margin: 0;
}

#postCommentBtn {
    width: auto;
    background: transparent;
    color: #0095f6;
    margin: 0;
    padding: 0;
    font-weight: bold;
    cursor: pointer;
}

.sheet-overlay {
    display: none;
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0,0,0,0.5);
    z-index: 2004;
}

/* --- 2. PROFILE HEADER & STATS --- */
.profile-stats-container {
    display: flex;
    justify-content: space-around;
    padding: 20px 10px;
    border-bottom: 1px solid #333;
    text-align: center;
}

.stat-item b {
    display: block;
    font-size: 18px;
    color: #fff;
}

.stat-item span {
    font-size: 13px;
    color: #999;
}

.profile-actions {
    display: flex;
    gap: 10px;
    padding: 10px;
}

.edit-btn, .follow-btn {
    flex: 1;
    background: #333;
    color: white;
    border: none;
    padding: 8px;
    border-radius: 5px;
    font-weight: bold;
    cursor: pointer;
    transition: 0.2s;
}

.edit-btn:active, .follow-btn:active {
    opacity: 0.7;
}

.follow-btn {
    background: #0095f6;
}

/* --- 3. PROFILE GRID (3x3 Square Photos) --- */
#userPostGrid {
    display: grid; 
    grid-template-columns: repeat(3, 1fr); 
    gap: 2px; 
    margin-top: 20px;
}

#userPostGrid img {
    width: 100%;
    aspect-ratio: 1 / 1;
    object-fit: cover;
}

/* --- 4. REELS VIEW (Full Screen Snap) --- */
#reelsView {
    display: none; 
    scroll-snap-type: y mandatory;
    overflow-y: scroll;
    height: 100vh;
    width: 100%;
    background: #000;
    scrollbar-width: none;
    -ms-overflow-style: none;
    -webkit-overflow-scrolling: touch; 
}

#reelsView::-webkit-scrollbar {
    display: none;
}

.reel-video-container {
    width: 100%;
    height: 100vh; 
    scroll-snap-align: start;
    scroll-snap-stop: always;
    position: relative;
    background: #000;
}

.reel-video-container video {
    width: 100%;
    height: 100%;
    object-fit: cover;
}

/* --- 5. REELS SIDEBAR & ANIMATIONS --- */
.reel-sidebar {
    position: absolute;
    right: 15px;
    bottom: 100px;
    display: flex;
    flex-direction: column;
    gap: 20px;
    align-items: center;
    z-index: 20;
}

.reel-sidebar i {
    font-size: 28px;
    color: white;
    text-shadow: 0 0 5px rgba(0,0,0,0.5);
    cursor: pointer;
}

.reel-sidebar span {
    font-size: 12px;
    color: white;
    margin-top: 5px;
}

.heart-animation {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%) scale(0);
    font-size: 100px;
    color: white;
    opacity: 0;
    pointer-events: none;
    z-index: 15;
}

.heart-pop {
    animation: heartPop 0.8s ease-out;
}

@keyframes heartPop {
    0% { transform: translate(-50%, -50%) scale(0); opacity: 0; }
    50% { transform: translate(-50%, -50%) scale(1.2); opacity: 0.9; }
    100% { transform: translate(-50%, -50%) scale(1); opacity: 0; }
}
